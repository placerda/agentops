version: 1
agent: https://aca-agent-run25054470832.niceriver-6fd14a29.eastus2.azurecontainerapps.io
dataset: ../../scripts/e2e_data/tools.jsonl
protocol: http-json
request_field: message
response_field: text
tool_calls_field: tool_calls
# Permissive thresholds: e2e smoke test of the http-json + tool-calling
# invocation path against a real LLM, not a quality gate for the model.
thresholds:
  tool_call_accuracy: ">=0"
  coherence: ">=0"
  fluency: ">=0"
  f1_score: ">=0"
  avg_latency_seconds: "<=60"
